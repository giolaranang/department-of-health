fix view training pax in employee login/
add more infor on trainings joined view/
add training name for delete participants in coordinator/
add training name for add participants in coordinator/
add checked by name in more ->training, coordinator/
add category and level in number of paxes for all/
change color of status for approved trainings(green) disaproved trainings(red) and waiting for changes(gray)
add checked by name in expand ->all trainings, coordinator/
employee approvals change header of checkboxes to "Choose Employee/s", hra/
training apporvals review change date interface to textboxes instead of dropdowns, hra/
admin make add user interface nice, admin/
